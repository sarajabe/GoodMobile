{
  "name": "good-mobile",
  "version": "1.0.0",
  "scripts": {
    "ng": "ng",
    "start": "npm run start:local",
    "start:local": "ng serve -c local --port 4000",
    "start:dev": "ng serve -c dev  --port 3000",
    "start:test": "ng serve -c test",
    "start:prod": "ng serve -c production",
    "build": "ng build",
    "build:local": "ng build -c local",
    "build:dev": "ng build -c dev --crossOrigin=use-credentials",
    "build:test": "ng build -c test --crossOrigin=use-credentials",
    "build:stage": "ng build -c stage",
    "build:prod": "ng build -c production",
    "test": "ng test",
    "lint": "ng lint"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^14.2.7",
    "@angular/common": "^14.2.7",
    "@angular/compiler": "^14.2.7",
    "@angular/core": "^14.2.7",
    "@angular/forms": "^14.2.7",
    "@angular/platform-browser": "^14.2.7",
    "@angular/platform-browser-dynamic": "^14.2.7",
    "@angular/router": "^14.2.7",
    "rxjs": "~7.5.0",
    "tslib": "^2.3.0",
    "zone.js": "~0.11.4"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^14.2.6",
    "@angular/cli": "^14.2.6",
    "@angular/compiler-cli": "^14.2.7",
    "@types/jasmine": "~3.10.0",
    "@types/node": "^12.11.1",
    "jasmine-core": "~4.0.0",
    "karma": "~6.3.0",
    "karma-chrome-launcher": "~3.1.0",
    "karma-coverage": "~2.1.0",
    "karma-jasmine": "~4.0.0",
    "karma-jasmine-html-reporter": "~1.7.0",
    "typescript": "~4.6.2"
  }
}